# CIFAR-10 卷积神经网络对比实验报告

## 1. 实验目的

说明本实验希望研究的问题：

- CNN 相比 MLP 的优势；
- BatchNorm 对优化过程的影响；
- Dropout 对过拟合的影响；
- 数据增强对泛化能力的影响；
- 残差结构的基本思想。

---

## 2. 数据集

### 2.1 数据集介绍

- 数据集：CIFAR-10；
- 输入形状：`[3, 32, 32]`；
- 类别数量：10；
- 训练、验证、测试样本数量：填写本地运行输出。

### 2.2 类别名称

```text
airplane, automobile, bird, cat, deer,
dog, frog, horse, ship, truck
```

### 2.3 数据划分

说明：

- 如何从官方训练集划分训练集和验证集；
- 为什么使用分层划分；
- 为什么测试集不能用于选择实验方案。

---

## 3. 数据预处理

### 3.1 标准化

填写使用的 mean 和 std，并解释按通道标准化。

### 3.2 数据增强

Exp 4 使用：

- RandomCrop；
- RandomHorizontalFlip。

解释为什么只对训练集使用随机增强。

---

## 4. 模型结构

### 4.1 MLP 对照模型

填写：

- 输入展平尺寸；
- 隐藏层；
- 参数量；
- MLP 对图像空间结构的限制。

### 4.2 BasicCNN

填写每层：

| 层 | 输入形状 | 输出形状 | 参数量 |
|---|---|---|---:|
| Conv Block 1 | | | |
| Conv Block 2 | | | |
| Conv Block 3 | | | |
| Linear 1 | | | |
| Output | | | |

### 4.3 LeNet、VGG 与 ResNet

简要说明三个架构的核心思想，不需要把它们与控制变量实验混成同一结论。

---

## 5. 实验设置

| 配置 | 数值 |
|---|---|
| Random seed | 42 |
| Epochs | |
| Batch size | |
| Optimizer | Adam |
| Learning rate | |
| Weight decay | |
| Loss | CrossEntropyLoss |
| Main validation metric | Accuracy |
| Device | |
| AMP | |

---

## 6. 核心对比实验

| 实验 | BN | Dropout | Augmentation | 参数量 | 最佳 Epoch | 最佳 Val Acc | 最终 Train Acc | 最终 Val Acc | 泛化差距 | 训练时间 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Exp 1 | 否 | 0.0 | 否 | | | | | | | |
| Exp 2 | 是 | 0.0 | 否 | | | | | | | |
| Exp 3 | 是 | 0.5 | 否 | | | | | | | |
| Exp 4 | 是 | 0.5 | 是 | | | | | | | |

---

## 7. 训练曲线分析

### 7.1 Exp 1：CNN 基线

分析：

- 收敛速度；
- 是否过拟合；
- 最佳 epoch。

### 7.2 BatchNorm

比较 Exp 1 与 Exp 2，分析：

- 前期 loss；
- 曲线稳定性；
- 最佳验证准确率；
- 参数量与耗时变化。

### 7.3 Dropout

比较 Exp 2 与 Exp 3，分析：

- 训练准确率；
- 验证准确率；
- 泛化差距；
- 是否缓解过拟合。

### 7.4 数据增强

比较 Exp 3 与 Exp 4，分析：

- 训练是否变难；
- 验证表现；
- 泛化差距；
- 结论的适用范围。

---

## 8. 最终模型选择

只根据验证集结果说明选择哪个实验作为最终模型。

必须写清：

- 选择依据；
- 为什么不是根据测试集选择；
- 是否考虑准确率以外的训练时间和模型复杂度。

---

## 9. 最终测试结果

| 指标 | 数值 |
|---|---:|
| Test Loss | |
| Test Accuracy | |
| Mean Per-Class Accuracy | |

说明验证准确率与测试准确率是否接近。

---

## 10. 每类别准确率与混淆矩阵

填写表现最好和最差的类别，并分析常见混淆：

- cat / dog；
- automobile / truck；
- airplane / ship；
- 其他本地结果中出现的混淆。

---

## 11. 错误分析

至少检查 10 个错误样本，记录：

| 样本 | 真实类别 | 预测类别 | 置信度 | 可能原因 |
|---:|---|---|---:|---|
| | | | | |

将错误分为：

- 图像模糊；
- 主体过小；
- 背景干扰；
- 类间视觉相似；
- 标签可能存在歧义；
- 模型表达能力不足。

---

## 12. 结论

回答：

1. CNN 是否明显优于 MLP；
2. BatchNorm 在当前实验中产生了什么影响；
3. Dropout 是否缓解过拟合；
4. 数据增强是否提高泛化；
5. 最终模型的主要不足；
6. 下一步最值得尝试什么。

---

## 13. 实验局限

至少说明：

- 只使用一个随机种子；
- 训练轮数有限；
- 超参数搜索有限；
- 模型规模较小；
- 结论只针对当前数据、架构和训练配置。
